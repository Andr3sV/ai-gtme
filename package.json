{
  "name": "ai-gtme",
  "private": true,
  "scripts": {
    "dev": "pnpm --filter backoffice dev",
    "build": "pnpm -r run build",
    "db:generate": "pnpm --filter backoffice db:generate",
    "db:migrate": "pnpm --filter backoffice db:migrate"
  },
  "devDependencies": {
    "typescript": "^5.6.3"
  },
  "packageManager": "pnpm@9.14.2",
  "engines": {
    "node": ">=20"
  }
}
